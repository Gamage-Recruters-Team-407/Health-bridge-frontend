"use client";

import { CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { PreventiveCarePoint } from "@/types/analytics";

export default function PreventiveCareTrend({ data }: { data: PreventiveCarePoint[] }) {
  return <div className="h-64 w-full"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{ top: 10, right: 8, left: -20, bottom: 0 }}><CartesianGrid stroke="#e2e8f0" strokeDasharray="4 4" vertical={false} /><XAxis dataKey="month" tickLine={false} axisLine={false} tick={{ fill: "#94a3b8", fontSize: 12 }} /><YAxis yAxisId="volume" tickLine={false} axisLine={false} tick={{ fill: "#94a3b8", fontSize: 12 }} /><YAxis yAxisId="rate" orientation="right" domain={[0, 100]} tickFormatter={(value: number) => `${value}%`} tickLine={false} axisLine={false} tick={{ fill: "#94a3b8", fontSize: 12 }} /><Tooltip formatter={(value: unknown, name: string | number | undefined) => [name === "followUpCompletion" ? `${Number(value ?? 0)}%` : Number(value ?? 0).toLocaleString(), name === "screenings" ? "Health Screenings" : name === "routineCheckups" ? "Routine Checkups" : "Follow-up Completion"]} contentStyle={{ border: "1px solid #e2e8f0", borderRadius: 10, fontSize: 12 }} cursor={{ stroke: "#bfdbfe", strokeWidth: 1 }} /><Legend wrapperStyle={{ fontSize: 11, paddingTop: 8 }} /><Line yAxisId="volume" type="monotone" dataKey="screenings" name="Health Screenings" stroke="#2563eb" strokeWidth={2.5} dot={false} /><Line yAxisId="volume" type="monotone" dataKey="routineCheckups" name="Routine Checkups" stroke="#0d9488" strokeWidth={2.5} dot={false} /><Line yAxisId="rate" type="monotone" dataKey="followUpCompletion" name="Follow-up Completion" stroke="#f59e0b" strokeWidth={2.5} dot={false} /></LineChart></ResponsiveContainer></div>;
}
