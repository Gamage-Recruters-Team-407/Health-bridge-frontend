import type { AnalyticsReport } from "@/types/analytics";
import ReportStatusBadge from "@/components/analytics/reports/ReportStatusBadge";

export default function RecentReportsTable({ reports, onView, onDownload }: { reports: AnalyticsReport[]; onView: (report: AnalyticsReport) => void; onDownload: (report: AnalyticsReport) => void }) {
  return <div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left text-sm"><thead className="border-b border-slate-200 text-xs text-slate-400"><tr>{["Report Name", "Category", "Period", "Generated Date", "Format", "Status", "Actions"].map((heading) => <th key={heading} className="px-4 py-3 font-medium first:pl-0">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{reports.map((report) => <tr key={report.id} className="text-slate-600"><td className="px-4 py-3.5 pl-0 font-semibold text-slate-800">{report.name}</td><td className="px-4 py-3.5">{report.category}</td><td className="px-4 py-3.5">{report.period}</td><td className="px-4 py-3.5">{report.generatedDate}</td><td className="px-4 py-3.5">{report.format}</td><td className="px-4 py-3.5"><ReportStatusBadge status={report.status} /></td><td className="px-4 py-3.5"><div className="flex gap-2"><button type="button" onClick={() => onView(report)} className="rounded-lg bg-blue-50 px-2.5 py-1.5 text-xs font-semibold text-blue-700 hover:bg-blue-100">View</button><button type="button" onClick={() => onDownload(report)} className="rounded-lg border border-slate-200 px-2.5 py-1.5 text-xs font-semibold text-slate-600 hover:border-blue-200 hover:text-blue-700">Download</button></div></td></tr>)}</tbody></table></div>;
}
