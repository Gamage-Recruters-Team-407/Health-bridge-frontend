import type { ReportSummaryDTO } from "@/types/analytics";
import ReportStatusBadge from "@/components/analytics/reports/ReportStatusBadge";

export default function RecentReportsTable({ reports, onView }: { reports: ReportSummaryDTO[]; onView: (report: ReportSummaryDTO) => void }) {
  return <div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left text-sm"><thead className="border-b border-slate-200 text-xs text-slate-400"><tr>{["Report Name", "Category", "Report Type", "Generated Date", "Format", "Status", "Actions"].map((heading) => <th key={heading} className="px-4 py-3 font-medium first:pl-0">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{reports.map((report) => <tr key={report.reportId} className="text-slate-600"><td className="px-4 py-3.5 pl-0 font-semibold text-slate-800">{report.title}</td><td className="px-4 py-3.5">{report.category}</td><td className="px-4 py-3.5">{report.reportType}</td><td className="px-4 py-3.5">{new Intl.DateTimeFormat(undefined, { dateStyle: "medium" }).format(new Date(report.generatedDate))}</td><td className="px-4 py-3.5">{report.format}</td><td className="px-4 py-3.5"><ReportStatusBadge status={report.status} /></td><td className="px-4 py-3.5"><button type="button" onClick={() => onView(report)} className="rounded-lg bg-blue-50 px-2.5 py-1.5 text-xs font-semibold text-blue-700 hover:bg-blue-100">View</button></td></tr>)}</tbody></table></div>;
}
