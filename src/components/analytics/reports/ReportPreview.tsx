import type { AnalyticsReport } from "@/types/analytics";
import ReportStatusBadge from "@/components/analytics/reports/ReportStatusBadge";

export default function ReportPreview({ report }: { report?: AnalyticsReport }) {
  if (!report) return <div className="flex min-h-[220px] items-center justify-center rounded-xl border border-dashed border-slate-200 bg-slate-50/70 p-6 text-center"><div><div className="mx-auto flex h-10 w-10 items-center justify-center rounded-full bg-blue-50 text-blue-600" aria-hidden="true">+</div><p className="mt-3 text-sm font-semibold text-slate-700">Select or generate a report to preview its summary.</p><p className="mt-1 text-xs text-slate-400">Preview content is generated from local analytics mock data.</p></div></div>;
  return <div className="space-y-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.14em] text-blue-600">{report.category}</p><h3 className="mt-1 text-lg font-bold text-slate-900">{report.name}</h3><p className="mt-1 text-xs text-slate-400">{report.period} · Generated {report.generatedDate}</p></div><ReportStatusBadge status={report.status} /></div><p className="rounded-xl bg-slate-50 p-3 text-sm leading-6 text-slate-600">{report.summary}</p><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{report.metrics.map((metric) => <div key={metric.label} className="rounded-xl border border-slate-100 p-3"><p className="text-[11px] text-slate-400">{metric.label}</p><p className="mt-1 text-base font-bold text-slate-800">{metric.value}</p></div>)}</div><p className="text-[11px] text-slate-400">Preview only. Full PDF and Excel export will be available after reporting backend integration.</p></div>;
}
